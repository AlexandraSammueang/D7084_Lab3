# Lab 3: Lab3_Explainability_and_BRBES

## Requirements

Install SHAP and LIME if they are not already available:

```bash
python -m pip install shap lime
```

## Running the Notebook

1. Place the dataset on your computer.
2. Open `notebook.ipynb` in Jupyter Notebook 
3. Update the dataset path in the data-loading cell:

```python
file = "path/to/Friday-WorkingHours-Afternoon-DDos.pcap_ISCX.csv"
```
4. Run the notebook cells in order.

